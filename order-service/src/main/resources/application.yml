micronaut:
  application:
    name: order-service

kafka:
  bootstrap:
    servers: localhost:9092

endpoints:
  all:
    enabled: true
    sensitive: false

tracing:
  zipkin:
    enabled: true
    http:
      url: http://localhost:9411
    sampler:
      probability: 1