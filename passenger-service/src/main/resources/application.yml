micronaut:
  application:
    name: passenger-service

kafka:
  bootstrap:
    servers: localhost:9092

tracing:
  zipkin:
    enabled: true
    http:
      url: http://localhost:9411
    sampler:
      probability: 1